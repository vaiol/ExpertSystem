const { Algorithm } = require('./Alghorithm');
const { Fact } = require('./Fact');

const extractNewFacts = (str) => {};


class Rule {
    constructor(rule, parent) {}
    processRule(self) {}
	collectNewFacts(self, facts, implication) {}
}

module.exports = {
    extractNewFacts,
    Rule
};



